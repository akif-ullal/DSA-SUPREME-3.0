
#include<iostream>
#include<deque>
using namespace std;

int main()
{
  // queue<int>q;

  // q.push(10);
  // q.push(20);
  // q.push(30);
  // q.push(40);
  // q.push(50);

  // cout<<q.front()<<endl;

  // cout<<q.size()<<endl;

  // cout<<q.empty()<<endl;

  // q.pop();

  // cout<<q.front()<<endl;

  deque<int>q;

  q.push_back(10);
  q.push_back(20);
  q.push_back(30);
  q.push_back(40);
  q.push_back(50);

  cout<<q.front()<<endl;

  cout<<q.size()<<endl;

  cout<<q.empty()<<endl;

  q.pop_front();

  cout<<q.front()<<endl;
  cout<<q.back()<<endl;

  q.push_front(100);
  cout<<q.front()<<endl;

}
